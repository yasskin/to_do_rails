<%= render('layouts/errors', :object => @task) %>

<%= form_for [@list, @task], remote: true do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <p><%= f.submit %></p>
<% end %>
